---
import { Picture } from 'astro:assets';
interface Props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---
<div class="picture-card">
	<Picture src={src} alt={alt} width={550} height={350} layout="constrained" fit="cover"/>
</div>
